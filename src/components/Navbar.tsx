/**
 * Navbar — DEPRECATED for authenticated routes.
 * Authenticated navigation is now handled by AppShell + AppSidebar.
 * This file is kept for backwards compatibility but is no longer rendered.
 */
export default function Navbar() {
  return null;
}

export type { AppUser } from "../lib/auth";
